// Collection Manager - Export apenas de tipos (componentes ainda não implementados nesta pasta)
// Mantido para evitar erros de compilação até que os componentes sejam adicionados.

export type {
  CollectionManagerProps,
  CollectionFormProps,
  CollectionItemManagerProps,
  BulkActionsProps,
} from './CollectionManager.types';